package main

import (
	"fmt"
	"os"
)

func main() {
	if len(os.Args) < 2 {
		fmt.Println("for to go yo should be say 'pin hop <name save>")
		return
	}
	/*
			p := model.NewPathMap(nil)
			p.AddPath("ws1", "/home/sion/Desktop/workspace/Actual/workspace/")
		repo := repository.NewRepository()
		p, err := repo.Get()
		if err != nil {
			log.Panic(err)
		}
	*/
}